import { TemplateTableType } from '@/types/schemas.types';
import React from 'react';

interface Props {
  initialTemplate: TemplateTableType;
}

const DayList = ({ initialTemplate }: Props) => {
  return <div>DayList</div>;
};

export default DayList;
